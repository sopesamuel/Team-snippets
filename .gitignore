# Environment variables
.env
.env.local

# Frontend build output
frontend/dist
frontend/node_modules
frontend/.vite

# TLS certificates (already have tls/.gitignore but just in case)
tls/

# Go binary
backend/bin

# OS junk
.DS_Store